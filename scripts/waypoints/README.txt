FIRST VOLO MORPHOLOGY — WAYPOINT PDF BUILDERS
Saved 2026-08-27

Recommended permanent repo location:
  scripts/waypoints/

FILES
-----
build_flight_a_waypoints_REAL_CLAY_BASES.py
  Final Flight A builder.
  Uses the user's real individual clay base tiles in:
    waypoints/flight-a/flight-a-bases/
  Uses meaning images in:
    waypoints/flight-a/images/
  Builds the individual Flight A PDFs, review packet, and ZIP.

build_flight_b_waypoint_set.py
  Flight B Waypoint set builder.

build_conformity_waypoint.py
  Original/master Conformity builder used while establishing the Flight B
  Waypoint visual template. Keep as a reference even if the set builder changes.

IMPORTANT
---------
Do not replace real Morpho prefix/root/suffix tiles with generated substitutes.
For REMOVABLE, preserve:
  remove + -able
NOT:
  re- + move + -able
